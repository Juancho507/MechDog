# Meshes

Place collision/visual meshes here (e.g. `base_link.stl`, `leg_*.stl`, `lidar.stl`) when CAD assets are available.

The current URDF uses primitive boxes/cylinders so the package builds without mesh files.
